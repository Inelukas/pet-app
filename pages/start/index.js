import styled from "styled-components";

export const StyledStartPage = styled.section`
  background-color: var(--primary-color);
`;

export default function StartPage() {
  return <StyledStartPage>Servus</StyledStartPage>;
}
